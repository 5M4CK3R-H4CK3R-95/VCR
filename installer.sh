echo "Please Wait While installing vcr .."
sleep 2
chmod +x *
mv vcr /bin
mv data /bin
cd ..
rm -rf VCR
echo "Done Vcr is installed now just type vcr to start only run vcr there where you apk is .."
exit
